import * as React from 'react';
import { useState, useEffect } from 'react';

const Works = ({ windowWidth }: { windowWidth: number }): JSX.Element => {
  return <div id='page_works'>헉</div>;
};

export default Works;
